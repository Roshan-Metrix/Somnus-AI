class: ChatMessage
table: chat_messages
fields:
  sessionId: String
  userId: int
  role: String
  content: String
  timestamp: DateTime
  embedding: Vector(768)?
  widgetType: String?
  widgetData: String?
indexes:
  chat_messages_embedding_idx:
    fields: embedding
    type: hnsw

